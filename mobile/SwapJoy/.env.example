# Copy to `.env` here for the Expo app. Do not commit `.env`.
#
# Supabase → Settings → API
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Database password / DATABASE_URL: not used by the mobile app.
# For CLI scripts (migrations, Pexels generator), put DATABASE_URL in the repo root `.env`
# (see ../../.env.example).
